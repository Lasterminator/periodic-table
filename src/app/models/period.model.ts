/**
 * Represents a row of chemical elements in the periodic table.
 */
export interface Period {
    /** The number of period row in the periodic table. */
    index: number,
    /** A brief description of the period. */
    description: string,
}